@using PRO219_WebsiteBanDienThoai_FPhone.ViewModel
@model List<PhoneDetailsViewModel>

@{
    ViewData["Title"] = "Bill Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
    String dh0 = "Đã xác nhận";
    String dh1 = "Đang bảo hành";
    String dh2 = "Hoàn trả";
    String dh3 = "Đã hoàn trả";
}

<table class="table table-bordered container">
    <thead>
        <tr>
            <td colspan="5" style="text-align:center">Bill Detail</td>
        </tr>
        <tr>
            <th>
                Phone Name
            </th>
            <th>
                Status
            </th>
            <th>
                Status Payment
            </th>
            <th>
                Total Money
            </th>
            <th>
                Detail
            </th>
        </tr>

    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @*@item?.PhoneDetaild?.Rams*@
                </td>
                <td>
                    @item.PhoneName | @item.RamName/@item.RomName | @item.ColorName

                </td>
                <td>
                </td>
                <td>
                    @*@item.IdPhoneDetail*@
                </td>
                @* <td>
            @if (item.Status == 1)
            {



            <p class="text-success"> @dh1</p>
            }
            else if (item.Status == 2)
            {
            <p class="text-danger"> @dh2</p>
            }
            else if (item.Status == 3)
            {
            <p class="text-danger"> @dh3</p>
            }
            else if (item.Status == 4)
            {
            <p class="text-success"> @dh4</p>
            }
            else if (item.Status == 5)
            {
            <p class="text-success"> @dh5</p>
            }
            </td>
            <td>
            @item.deliveryPaymentMethod  @if (item.StatusPayment == 1)
            {
            <span class="text-success">Đã thanh toán </span>
            }
            else
            {
            <span class="text-danger">Chưa thanh toán </span>
            }
            </td>
            <td>
            @string.Format("{0:0,0}", item.TotalMoney) vnđ
            </td>
            <td>
            <a href="@Url.Action("Detail", "Bill", new { id = item.Id })">Xem Chi tiết</a>
            </td>*@
            </tr>
        }
    </tbody>
</table>